<?xml version="1.0" encoding="ISO-8859-1" standalone="yes"?>
<!DOCTYPE funcs SYSTEM "../../../clonk.dtd">
<?xml-stylesheet type="text/xsl" href="../../../clonk.xsl"?>
<funcs>
  <func>
    <title>GetEffect</title>
    <category>Effekte</category>
    <version>4.9.5.0 CE</version>
    <syntax>
      <rtype>any</rtype>
      <params>
        <param>
          <type>string</type>
          <name>name</name>
          <desc>Name des Effektes ohne vorangestelltes Fx. Es können '*' und '?' als Suchmaske verwendet werden.</desc>
          <optional />
        </param>
        <param>
          <type>object</type>
          <name>target</name>
          <desc>Zielobjekt, in dem Effekte abgefragt werden sollen. Bei 0 wird die globale Effektliste durchsucht.</desc>
          <optional />
        </param>
        <param>
          <type>int</type>
          <name>index</name>
          <desc>Effektindex. Bei angegebenem name gibt dies bei mehreren, der Suchmaske entsprechenden Effekten den indizierten Effekt in der Liste der passenden Effekte an. Ist name nicht angegeben, wird dagegen der Effekt mit der entsprechenden Effektnummer abgefragt. Im Gegensatz zu Effektnummern ist bei Indizes stets sichergestellt, dass beim Hochzählen von 0 bis zur Effektzahl alle Effekte erfasst werden.</desc>
          <optional />
        </param>
        <param>
          <type>int</type>
          <name>query_value</name>
          <optional />
          <desc>Index des Wertes, der abgefragt werden soll. Siehe Anmerkung.</desc>
        </param>
        <param>
          <type>int</type>
          <name>max_priority</name>
          <optional />
          <desc>Wenn ungleich 0, werden nur Effekte zurückgegeben, die höchstens die angegebene Priorität haben.</desc>
        </param>
      </params>
    </syntax>

    <desc>Fragt Effektdaten ab, oder ermittelt, ob ein bstimmter Effekt vorhanden ist. Der Rückgabetyp richtet sich nach dem abgefragten Wert. Wurde der Effekt nicht gefunden, wird immer 0 zurückgegeben.</desc>

    <remark>Über den Parameter query_value können folgende Werte abgefragt werden:<br/><br/>
      <table>
        <rowh><col>query_value</col><col>Wert</col></rowh>
        <row><col>0</col><col>Effektnummer</col></row>
        <row><col>1</col><col>Name</col></row>
        <row><col>2</col><col>Priorität</col></row>
        <row><col>3</col><col>Timer-Intervall</col></row>
        <row><col>4</col><col>Befehlsziel</col></row>
        <row><col>5</col><col>Befehlsziel-ID</col></row>
        <row><col>6</col><col>Laufzeit des Effektes</col></row>
      </table>
    </remark>

    <examples>
      <example>
        <code>var obj, i, effect;
// Alle Objekte durchsuchen
<funclink>while</funclink> (obj=<funclink>FindObject</funclink>(0, 0,0,0,0, 0, 0,0, 0, obj))
  {
  // Von Effektzahl abwärts zählen, da Effekte entfernt werden
  i = <funclink>GetEffectCount</funclink>(0, obj);
  <funclink>while</funclink> (i--)
    <funclink>if</funclink> (effect = GetEffect("*Spell", obj, i))
      <funclink>RemoveEffect</funclink>(0, obj, effect);
  }
// Globale Effekte entfernen
i = <funclink>GetEffectCount</funclink>();
<funclink>while</funclink> (i--)
  <funclink>if</funclink> (effect = GetEffect("*Spell", 0, i))
    <funclink>RemoveEffect</funclink>(0, 0, effect);</code>
        <text>Entfernt alle Zaubereffekte von allen Objekten und global.</text>
      </example>
    </examples>

    <related>
      <emlink href="script/Effects.html">Effekte-Dokumentation</emlink>
      <funclink>AddEffect</funclink>
      <funclink>ChangeEffect</funclink>
      <funclink>CheckEffect</funclink>
      <funclink>GetEffectCount</funclink>
      <funclink>EffectCall</funclink>
      <funclink>EffectVar</funclink>
      <funclink>RemoveEffect</funclink>
    </related>
  </func>
  <author>Sven2</author><date>März 2004</date>
</funcs>
